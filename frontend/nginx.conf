server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Uygulaman artık /ehp altında servis edilecek
    location /ehp/ {
        alias /usr/share/nginx/html/; # root yerine alias bazen daha kararlıdır
        try_files $uri $uri/ /ehp/index.html;
    }

    # --- YENİ EKLENEN KISIM: BACKEND PROXY ---
    location /ehp/api/ {
        # 'backend' docker-compose'daki servis isminizdir.
        # Konteynerlar arası iletişimde iç port (3000) kullanılır.
        proxy_pass http://backend:3000/; 
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
    # -----------------------------------------

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        try_files $uri =404;
    }
}
